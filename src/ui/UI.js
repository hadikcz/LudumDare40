'use strict';
import 'phaser';
export default class {
    /**
     *
     * @param {Phaser.Game} game
     */
    constructor (
        game
    ) {
        this.game = game;

        // this.teamSelectUI = new TeamSelectUI(this.game, teamHandler);
    }
}
